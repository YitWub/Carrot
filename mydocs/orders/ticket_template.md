# [티켓 번호] 작업 타이틀 (예: [M1-01] 회원 탈퇴 API 구현)

## 1. 작업 개요
(이 티켓에서 구현할 기능의 목적과 요약을 작성합니다.)

## 2. 관련 엔티티 (DB 테이블)
- (예: `User`, `Product`)

## 3. API 명세 (Endpoint & Request/Response)
- **Method:** `POST` / `GET` / `PUT` / `DELETE`
- **URL:** `/api/v1/...`
- **Request Body:**
  ```json
  {
    "key": "value"
  }
  ```
- **Response Body:**
  ```json
  {
    "key": "value"
  }
  ```

## 4. 기능 요구사항 및 비즈니스 로직
- [ ] 요구사항 1
- [ ] 요구사항 2

## 5. 권한 요구사항
- (예: 로그인한 본인만 접근 가능)

## 6. 예외 처리 (Exception Handling)
- [ ] 예외 케이스 1: (예: 이미 탈퇴한 회원일 경우 400 에러 반환)
- [ ] 예외 케이스 2: (예: 필수 파라미터 누락 시 400 에러 반환)

## 7. 테스트 케이스 (Test Cases)
**성공 케이스:**
- [ ] (예: 정상적으로 탈퇴 처리 후 DB `is_deleted = true` 확인)

**실패 케이스:**
- [ ] (예: 잘못된 토큰으로 요청 시 401 에러 반환 확인)

## 8. 완료 조건 (Definition of Done - DoD)
- [ ] 모든 비즈니스 로직 구현 완료
- [ ] 수동/자동 테스트 케이스 100% 통과
- [ ] PRD(기획서)의 요구사항 완벽 충족
